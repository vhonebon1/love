import React from 'react';
import Loader from 'react-loader-spinner';

const Spinner = () =>
  <Loader
    type="ThreeDots"
    color="#136FFF"
    height="100"
    width="100"
  />

export default Spinner;
